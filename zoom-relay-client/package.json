{
  "name": "zoom-relay-client",
  "version": "0.0.0",
  "scripts": {
    "develop": "./node_modules/.bin/vite",
    "prebuild": "yarn",
    "build": "./node_modules/.bin/vite build",
    "postbuild": "rm -rf ../zoom-relay-server/public/* && mv ./dist/* ../zoom-relay-server/public",
    "preview": "./node_modules/.bin/vite preview"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "6",
    "simpledotcss": "^1.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^1.0.7",
    "vite": "^2.7.2"
  },
  "engines": {
    "node": "14.x"
  }
}
